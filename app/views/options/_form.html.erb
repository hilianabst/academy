<%= form_with(model: [@question.quiz.training,@question.quiz,@question, option], local: true) do |form| %>
  <% if option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(option.errors.count, "error") %> prohibited this option from being saved:</h2>

      <ul>
      <% option.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_field :text %>
  </div>

  <div class="field">
    <%= form.label :is_correct %>
    <%= form.check_box :is_correct %>
  </div>



  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
